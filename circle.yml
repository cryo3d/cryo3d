machine:
  timezone:
    America/Denver

deployment:
  staging:
    branch: circle_ci
    commands:
      - sudo ln -s /etc/apparmor.d/docker /etc/apparmor.d/force-complain/
      - sudo docker daemon
      - docker build -t some-content-nginx .
      - docker tag some-content-nginx cryo3d/cryo3d
      - docker push cryo3d/cryo3d